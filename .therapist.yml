actions:
  eslint:
    run: yarn eslint --max-warnings 0 {files}
    include:
      - "*.js"
      - "*.jsx"
  stylelint:
    run: yarn stylelint {files}
    include:
      - "*.css"
      - "*.less"
